Place your certificate images (png/jpg/jpeg/svg) in this folder.

The `CertificateSlider` component will automatically load them via Vite's import.meta.glob and display them in the footer.

Recommended image height: around 300-600px. Filenames can be any valid name.
